# 项目约定

## 知识库与服务器同步（强制）

每完成一处**有部署意义**的改动（新/改前后端源码、路由、页面、依赖），必须：

1. 在 `D:\生活\知识库\开发\项目\自研\一站通\` 记**功能说明**：`YYYY.M.D主题.md`。
2. 另开**独立部署方案**（不要塞进旧的《服务器同步部署记录（持续追加）.md》）：`YYYY.M.D主题·部署方案.md`。内容含：变更文件、备份、部署步骤、验证清单、回滚、文末「执行结果」勾选。
3. 连上服务器按该部署方案执行；完成后在方案文档文末补执行结果。

服务器：192.168.101.3，scp 直传（**不是 git**）。容器/挂载细节可参考技能 `deploy-server` 与历史文档 B1–B4。  
《服务器同步部署记录（持续追加）.md》仅作历史基线，**新批次勿再往里加**。
