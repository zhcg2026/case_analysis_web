# 🔧 快速恢复服务器步骤

## 🚨 当前问题

后端 Docker 容器启动失败，报错 `Worker failed to boot`

## ✅ 立即恢复服务器的方法

在服务器上执行以下命令：

```bash
# 1. 进入项目目录
cd /root/case_analysis_web

# 2. 从 Git 恢复 app.py 到原始状态（语法正确的版本）
git checkout backend/app.py

# 3. 重启所有服务
docker-compose restart

# 或者如果还不行，重建容器
docker-compose down
docker-compose up -d

# 4. 查看日志确认服务正常
docker-compose logs -f case-analysis-app
```

## 📊 验证服务是否正常

```bash
# 查看容器状态
docker-compose ps

# 应该看到：
# NAME                  STATUS
# case-analysis-web     Up
# mysql-case-analysis   Up (healthy)

# 测试后端接口
curl http://localhost:5000/health

# 应该返回：{"status": "ok"}
```

## 🔍 如果还是不行

```bash
# 查看详细错误日志
docker-compose logs case-analysis-app | tail -100

# 检查 Python 语法
docker-compose exec case-analysis-app python -m py_compile backend/app.py
```

## 💡 临时解决方案

服务恢复后，如果还需要使用数据分析功能：

1. **在前端选择"阿里云百炼"模型**（已测试可以正常工作）
2. 暂时不要选择"火山引擎"模型
3. 等待后续修复

## 📝 后续计划

等服务器正常运行后，我会：
1. 创建一个经过充分测试的修复脚本
2. 在本地验证语法正确
3. 再部署到服务器

---

**重要提示**：先恢复服务器，让服务正常运行，修复工作可以稍后进行。
