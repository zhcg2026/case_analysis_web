# 🚨 紧急修复说明 - 火山引擎 API 错误

## 问题诊断

后端服务启动失败，报错 `Worker failed to boot`，这是因为之前的修复脚本有 bug，导致 Python 语法错误。

## 当前状态

✅ 本地文件 `backend/app.py` 已经恢复到原始状态（语法正确）
❌ 服务器上的文件可能有问题

## 📋 解决方案（3 选 1）

### 方案 1：使用 Git 自动部署（推荐）

如果你的服务器配置了 Git 自动部署：

```bash
# 在本地执行
cd c:\Users\Administrator\Documents\trae_projects\case_analysis_web
git add backend/app.py
git commit -m "Fix: 增强火山引擎 API 错误处理"
git push origin main
```

然后在服务器上：
```bash
cd /root/case_analysis_web
git pull origin main
docker-compose restart case-analysis-app
```

### 方案 2：手动上传文件

1. 使用 FTP/SFTP 工具（如 WinSCP、FileZilla）将本地文件上传到服务器：
   - 本地文件：`c:\Users\Administrator\Documents\trae_projects\case_analysis_web\backend\app.py`
   - 服务器路径：`/root/case_analysis_web/backend/app.py`

2. 在服务器上重启服务：
```bash
cd /root/case_analysis_web
docker-compose restart case-analysis-app
```

### 方案 3：直接在服务器上修改（不推荐）

如果你熟悉 vim/nano，可以直接在服务器上修改文件。

## ⚠️ 重要提示

由于自动修复工具存在 bug，**建议暂时不要修改代码**，先让服务正常运行。

火山引擎 API 调用失败的问题，可以通过以下方式临时解决：
- 在前端选择"阿里云百炼（通义千问）"模型而不是"火山引擎（豆包）"
- 阿里百炼已经测试过可以正常工作

## 📝 后续修复计划

等服务器正常运行后，我会创建一个可靠的修复脚本，通过以下方式：
1. 创建一个新的 Python 文件，包含所有修复
2. 使用 `git diff` 和 `git apply` 应用修复
3. 或者手动复制粘贴代码片段

## 🆘 如果服务器已经无法启动

在服务器上执行：

```bash
cd /root/case_analysis_web
# 查看当前状态
docker-compose ps

# 查看错误日志
docker-compose logs case-analysis-app

# 如果 app.py 有语法错误，从 Git 恢复
git checkout backend/app.py

# 重启服务
docker-compose restart case-analysis-app
```

## 联系支持

如果需要帮助，请提供：
1. `docker-compose logs case-analysis-app` 的完整输出
2. 前端控制台的错误信息
3. 网络请求的详细信息
