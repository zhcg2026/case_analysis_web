# 🔧 火山引擎 API 错误修复说明

## 问题诊断

`/api/analyze-v2` 接口调用火山引擎时**不稳定**（有时成功，有时失败），原因是：

1. **错误处理不完善** - 异常被吞掉，worker 继续执行但没有正确数据
2. **没有日志输出** - 无法诊断 API 调用是否成功
3. **超时处理不当** - API 卡住时 worker 会超时崩溃

## 需要修改的位置

### 文件：`backend/app.py`

**位置**：第 3589-3623 行（`/api/analyze-v2` 中的火山引擎调用部分）

**原代码**：
```python
        else:
            # 调用火山引擎（默认）
            try:
                headers = {
                    'Content-Type': 'application/json',
                    'Authorization': f'Bearer {API_KEY}'
                }
                
                payload = {
                    "model": MODEL,
                    "messages": [
                        {
                            "role": "system",
                            "content": chart_requirement_system_prompt
                        },
                        {
                            "role": "user",
                            "content": chart_requirement_prompt
                        }
                    ],
                    "temperature": 0.3,
                    "max_tokens": 2000
                }
                
                response = requests.post(
                    API_URL, 
                    headers=headers, 
                    json=payload, 
                    timeout=(10, 120)
                )
                response.raise_for_status()
                result = response.json()
                chart_requirement_text = result['choices'][0]['message']['content']
            except Exception as e:
                print(f"获取图表需求失败：{e}")
```

**修改为**：
```python
        else:
            # 调用火山引擎（默认）
            try:
                print(f"[火山引擎 - 图表需求] 开始调用 API")
                headers = {
                    'Content-Type': 'application/json',
                    'Authorization': f'Bearer {API_KEY}'
                }
                
                payload = {
                    "model": MODEL,
                    "messages": [
                        {
                            "role": "system",
                            "content": chart_requirement_system_prompt
                        },
                        {
                            "role": "user",
                            "content": chart_requirement_prompt
                        }
                    ],
                    "temperature": 0.3,
                    "max_tokens": 2000
                }
                
                print(f"[火山引擎 - 图表需求] 发送请求...")
                response = requests.post(
                    API_URL, 
                    headers=headers, 
                    json=payload, 
                    timeout=(10, 120)
                )
                
                print(f"[火山引擎 - 图表需求] 响应状态码：{response.status_code}")
                response.raise_for_status()
                result = response.json()
                chart_requirement_text = result['choices'][0]['message']['content']
                print(f"[火山引擎 - 图表需求] 成功获取图表需求")
            except requests.exceptions.Timeout as e:
                print(f"[火山引擎 - 图表需求] 调用超时：{e}")
                chart_requirement_text = None
                raise Exception(f"火山引擎 API 调用超时：{str(e)}")
            except requests.exceptions.RequestException as e:
                print(f"[火山引擎 - 图表需求] 网络错误：{e}")
                chart_requirement_text = None
                raise Exception(f"火山引擎 API 网络错误：{str(e)}")
            except Exception as e:
                print(f"[火山引擎 - 图表需求] 获取图表需求失败：{e}")
                import traceback
                traceback.print_exc()
                chart_requirement_text = None
                raise Exception(f"获取图表需求失败：{str(e)}")
```

## 修改内容

1. ✅ **添加详细日志** - 记录 API 调用的每个步骤
2. ✅ **区分错误类型** - 超时、网络错误、其他异常分别处理
3. ✅ **重新抛出异常** - 避免 worker 超时崩溃
4. ✅ **设置 chart_requirement_text = None** - 确保后续代码不会使用无效数据

## 部署步骤

### 在本地执行：

```bash
cd c:\Users\Administrator\Documents\trae_projects\case_analysis_web

# 1. 修改 backend/app.py（手动复制上面的代码）

# 2. 验证语法
python -m py_compile backend/app.py

# 3. 提交
git add backend/app.py
git commit -m "Fix: 增强火山引擎 API 错误处理（analyze-v2）"

# 4. 推送
git push origin main
```

### 在服务器上执行：

```bash
cd /root/case_analysis_web

# 1. 更新代码
git pull origin main

# 2. 重启服务
docker-compose restart case-analysis-app

# 3. 查看日志
docker-compose logs -f case-analysis-app | grep "火山引擎"
```

## 测试

在前端执行多次数据分析操作，观察：
1. 后端日志中是否有 `[火山引擎 - 图表需求]` 的详细输出
2. 是否还有 `ERR_EMPTY_RESPONSE` 错误
3. 分析结果是否稳定

## 预期效果

- ✅ 日志中能看到完整的 API 调用过程
- ✅ 失败时能看到具体的错误原因
- ✅ 不再出现 worker 超时崩溃
- ✅ 分析结果更稳定
