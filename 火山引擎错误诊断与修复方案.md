# 火山引擎 API 调用错误诊断与修复方案

## 问题描述

在数据分析（新版）功能中调用火山引擎时出现错误：
- **前端错误**: `POST http://81.70.163.116:5000/api/analyze-v2 net::ERR_EMPTY_RESPONSE`
- **控制台错误**: `Error analyzing data: TypeError: Failed to fetch`
- **对比**: 调用阿里百炼没有问题

## 诊断结果

### ✅ 已验证正常的部分

1. **火山引擎 API 配置正确**
   - API Key: `58a51ac5-3b75-4c5e-85ac-1fb4ef652bd0`
   - API URL: `https://ark.cn-beijing.volces.com/api/v3/chat/completions`
   - 模型：`doubao-seed-1-8-251228`
   - **测试结果**: API 连接正常，能成功返回响应

2. **阿里云百炼 API 工作正常**
   - 说明前端和后端的基本通信没有问题

### ❌ 可能的问题原因

根据 `ERR_EMPTY_RESPONSE` 错误特征，可能的原因有：

1. **后端服务崩溃或异常终止**
   - 火山引擎 API 调用过程中发生未捕获的异常
   - 导致 Flask 服务中断连接

2. **超时设置不当**
   - 火山引擎 API 响应时间较长
   - 默认的超时设置可能不够

3. **错误处理不完善**
   - 代码中某些异常没有被正确捕获
   - 导致连接被直接关闭而不是返回错误信息

4. **网络或防火墙问题**
   - 服务器到火山引擎 API 的网络不稳定
   - 防火墙阻止了某些请求

## 解决方案

### 方案 1：增强错误处理和日志记录（推荐）

已在 `app.py` 中进行了以下改进：

#### 1. 图表需求生成部分（第 3590-3623 行）

```python
else:
    # 调用火山引擎（默认）
    try:
        print(f"[火山引擎] 开始调用 API: {API_URL}")
        print(f"[火山引擎] 使用模型：{MODEL}")
        
        headers = {
            'Content-Type': 'application/json',
            'Authorization': f'Bearer {API_KEY}'
        }
        
        payload = {
            "model": MODEL,
            "messages": [
                {
                    "role": "system",
                    "content": chart_requirement_system_prompt
                },
                {
                    "role": "user",
                    "content": chart_requirement_prompt
                }
            ],
            "temperature": 0.3,
            "max_tokens": 2000
        }
        
        print(f"[火山引擎] 发送请求...")
        response = requests.post(
            API_URL, 
            headers=headers, 
            json=payload, 
            timeout=(10, 120)
        )
        
        print(f"[火山引擎] 响应状态码：{response.status_code}")
        print(f"[火山引擎] 响应内容前 500 字符：{response.text[:500]}...")
        
        response.raise_for_status()
        result = response.json()
        print(f"[火山引擎] 解析响应：{json.dumps(result, ensure_ascii=False)[:300]}...")
        
        chart_requirement_text = result['choices'][0]['message']['content']
        print(f"[火山引擎] 成功获取图表需求：{chart_requirement_text[:200]}...")
    except requests.exceptions.Timeout as e:
        print(f"[火山引擎] 调用超时：{e}")
        chart_requirement_text = None
        raise Exception(f"火山引擎 API 调用超时：{str(e)}")
    except requests.exceptions.RequestException as e:
        print(f"[火山引擎] 网络错误：{e}")
        chart_requirement_text = None
        raise Exception(f"火山引擎 API 网络错误：{str(e)}")
    except Exception as e:
        print(f"[火山引擎] 获取图表需求失败：{e}")
        import traceback
        traceback.print_exc()
        chart_requirement_text = None
        raise Exception(f"获取图表需求失败：{str(e)}")
```

#### 2. 分析报告生成部分（第 3879-3934 行）

添加详细的日志记录和错误处理，包括：
- 记录每次 API 调用的详细信息
- 区分超时、网络错误和其他异常
- 提供友好的错误消息

### 方案 2：调整超时设置

如果火山引擎 API 响应较慢，可以增加超时时间：

```python
# 原设置
timeout=(10, 120)  # 连接 10 秒，读取 120 秒

# 新设置（更长的超时时间）
timeout=(10, 300)  # 连接 10 秒，读取 300 秒
```

### 方案 3：添加重试机制

为图表需求生成部分添加重试机制（类似分析报告生成部分）：

```python
max_retries = 3
retry_delay = 5

for attempt in range(max_retries):
    try:
        response = requests.post(
            API_URL, 
            headers=headers, 
            json=payload, 
            timeout=(10, 120)
        )
        response.raise_for_status()
        result = response.json()
        chart_requirement_text = result['choices'][0]['message']['content']
        break
    except requests.exceptions.Timeout as e:
        if attempt < max_retries - 1:
            print(f"API 调用超时，{retry_delay}秒后重试... (尝试 {attempt+1}/{max_retries})")
            import time
            time.sleep(retry_delay)
            retry_delay *= 2
        else:
            raise Exception(f"火山引擎 API 调用超时：{str(e)}")
    except Exception as e:
        print(f"获取图表需求失败：{e}")
        raise
```

### 方案 4：前端增加错误提示

修改前端代码，给用户更友好的错误提示：

```javascript
const response = await fetch('/api/analyze-v2', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    ...getAuthHeaders()
  },
  body: JSON.stringify({
    table_name: selectedTableV2.value,
    prompt: analysisPrompt.value,
    model: selectedModel.value
  })
});

if (!response.ok) {
  const errorData = await response.json();
  throw new Error(errorData.error || '分析失败，请重试');
}
```

## 测试步骤

### 步骤 1：测试火山引擎 API 直连

```bash
cd backend
python test_volcengine.py
```

预期结果：✅ 火山引擎 API 连接正常

### 步骤 2：启动后端服务并查看日志

```bash
cd backend
python app.py
```

观察控制台输出，确保没有错误。

### 步骤 3：在前端测试数据分析功能

1. 打开前端页面
2. 选择数据表
3. 选择"火山引擎（豆包）"
4. 输入分析需求
5. 点击"开始分析"
6. **同时观察后端控制台的日志输出**

### 步骤 4：查看后端日志

如果仍然失败，查看后端控制台的详细日志：
- `[火山引擎]` 开头的日志显示 API 调用过程
- 错误信息会显示具体失败原因

## 常见问题排查

### 问题 1：后端服务崩溃

**症状**: 前端一直显示"加载中"，后端无任何日志

**解决**:
1. 检查后端服务是否正常启动
2. 查看是否有 Python 异常
3. 确保端口 5000 没有被占用

### 问题 2：API 调用超时

**症状**: 后端日志显示"调用超时"

**解决**:
1. 增加超时时间：`timeout=(10, 300)`
2. 检查网络连接
3. 考虑使用阿里云百炼作为备选

### 问题 3：防火墙阻止

**症状**: 后端日志显示"网络错误"或"连接错误"

**解决**:
1. 检查服务器防火墙设置
2. 确保可以访问 `https://ark.cn-beijing.volces.com`
3. 测试网络连通性：`ping ark.cn-beijing.volces.com`

### 问题 4：JWT Token 过期

**症状**: 返回 401 错误

**解决**:
1. 重新登录获取新 token
2. 检查 token 有效期设置

## 建议

1. **使用阿里云百炼作为默认选项**
   - 既然阿里云百炼工作正常，可以暂时将其设为默认
   - 火山引擎作为备选方案

2. **添加健康检查**
   - 在调用 API 前先测试连接
   - 如果火山引擎不可用，自动切换到阿里云百炼

3. **监控和告警**
   - 记录所有 API 调用失败
   - 设置失败率告警

## 修改文件清单

1. `backend/app.py` - 增强错误处理和日志记录
2. `backend/test_volcengine.py` - 火山引擎 API 测试脚本（新建）
3. `backend/debug_analyze_v2.py` - 端到端调试脚本（新建）
4. `前端/App.vue` - 可选：增强前端错误提示

## 联系支持

如果问题仍然存在，请提供：
1. 后端控制台的完整日志
2. 前端控制台的错误信息
3. 网络请求的详细信息
